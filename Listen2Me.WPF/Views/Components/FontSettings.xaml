<UserControl x:Class="Listen2Me.WPF.Views.Components.FontSettings"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="350">
    <ToolBarTray>
        <ToolBar Style="{StaticResource MaterialDesignToolBar}">
            <Label VerticalAlignment="Center" Content="Font Size:" />
            <ComboBox MinWidth="40"
                      ItemsSource="{Binding FontSizeSource, ElementName=Root}"
                      SelectedItem="{Binding SelectedFontSize, ElementName=Root, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Label Content="{Binding}" FontSize="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <ComboBox MinWidth="80"
                      ItemsSource="{Binding FontFamilySource, ElementName=Root}"
                      SelectedItem="{Binding SelectedFontFamily, ElementName=Root, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Label Content="{Binding}" FontFamily="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <ListBox materialDesign:ListBoxAssist.CanUserToggleSelectedItem="True" 
                     SelectionMode="Extended"
                     SelectionChanged="FontStyleListBox_SelectionChanged">
                <ListBoxItem x:Name="BoldItem" Content="{materialDesign:PackIcon Kind=FormatBold}" />
                <ListBoxItem x:Name="ItalicItem" Content="{materialDesign:PackIcon Kind=FormatItalic}" />
            </ListBox>
        </ToolBar>
    </ToolBarTray>
</UserControl>
